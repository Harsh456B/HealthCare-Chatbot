services:
  - type: web
    name: MedicalChatbot-1
    env: docker
    repo: https://github.com/Harsh456B/MedicalChatbot
    branch: main
    dockerContext: .
    dockerfilePath: Dockerfile
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 180 --graceful-timeout 60 app:app
    plan: free
